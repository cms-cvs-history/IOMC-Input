process PROD = {
#The module which reade from a HepMC ASCII file     
    source = MCFileSource 
    {  
# The HepMC test File
    untracked string fileName = "PythiaWriteHepMC.dat" 
    untracked int32 maxEvents = 2
    }
# The pool file where the HepMC product is stored 
    module out = PoolOutputModule { string fileName = "mcpool.root" }
    path p = { out }
}
