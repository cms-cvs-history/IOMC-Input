process PROD = {
#The module which reade from a HepMC ASCII file     
    source = MCFileSource 
    {  
	# The HepMC test File - normal ASCII
     	untracked vstring fileNames = {"file:SherpaWriteHepMC.dat"} 
	# The HepMC test File for heavy Ions - extended ASCII
     	#untracked vstring fileNames = {"file:hi05_hydjet_x1_b12_pt07_Extended_r000001.hepmc"} 
	# Set to true for heavy ions
	untracked bool useExtendedAscii =false
	#    untracked int32 maxEvents = 5	
    }
   untracked PSet maxEvents = {untracked int32 input = 2}
   # The pool file where the HepMC product is stored 
   module out = PoolOutputModule {untracked string fileName = "mcpool.root" }
   endpath p = { out }
}
